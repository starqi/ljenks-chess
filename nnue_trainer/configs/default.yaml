

# Common
batch_size: 1024
lr: 0.001
checkpoint_path: nnue_model.pt


# train_once.py
positions_path: xyz.bin
simple_epochs: 10


# repeat_train.py
cycles: 10
epochs_per_cycle: 1
#games_per_worker: 500
games_per_worker: 50
games_per_worker_validation_set: 1
workers: 5 # TODO (Minor) "half" or "full" meaning half of CPU count or all
max_nodes: 30000
random_half_moves: 20
max_half_moves: 300
val_every: 1
validation_path: validation.bin
checkpoint_backup_count: 2
